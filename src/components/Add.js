import React from 'react'
import Display from './Display';
import Controls from './Controls';

function Add(props) {
  return (
    <div>
      <Display />
      <Controls />
      <button>Add</button>
    </div>
  )
}

export default Add
